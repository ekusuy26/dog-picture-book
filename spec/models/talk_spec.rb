require 'rails_helper'

describe Talk do
  describe '#create' do

    # it "textがない場合は登録できないこと" do
    #   talk = build(:talk, text: "")
    #   talk.valid?
    #   expect(talk.errors[:text]).to include("can't be blank")
    # end

    # it "textがあれば登録できること " do
    #   talk = build(:talk, text: "aaaaaa")
    #   expect(talk).to be_valid
    # end

  end
end